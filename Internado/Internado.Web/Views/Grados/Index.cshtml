@model List<Internado.Infrastructure.Models.Grado>

@{
    ViewData["Title"] = "Grados Académicos";
}

<div class="container mt-4">
    <div class="row mb-4">
        <div class="col-md-8">
            <h2>📚 Grados Académicos</h2>
        </div>
        <div class="col-md-4 text-end">
            <a href="@Url.Action("Create")" class="btn btn-primary">
                ➕ Nuevo Grado
            </a>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">@TempData["Success"]</div>
    }

    @if (Model.Count == 0)
    {
        <div class="alert alert-info">No hay grados disponibles.</div>
    }
    else
    {
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>Nivel</th>
                    <th>Nombre</th>
                    <th>Descripción</th>
                    <th>Cursos Asignados</th>
                    <th>Estado</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var grado in Model)
                {
                    <tr>
                        <td>@grado.Nivel</td>
                        <td>@grado.Nombre</td>
                        <td>@grado.Descripcion</td>
                        <td>
                            <span class="badge bg-info">@grado.GradoCursos.Count(gc => gc.Activa)</span>
                        </td>
                        <td>
                            <span class="badge bg-@(grado.Estado == "Activo" ? "success" : "danger")">
                                @grado.Estado
                            </span>
                        </td>
                        <td>
                            <a href="@Url.Action("AsignarCursos", new { id = grado.Id })"
                               class="btn btn-sm btn-info">Cursos</a>
                            <a href="@Url.Action("Edit", new { id = grado.Id })"
                               class="btn btn-sm btn-warning">Editar</a>
                            <form asp-action="Delete" asp-route-id="@grado.Id"
                                  method="post" style="display:inline;">
                                <button type="submit" class="btn btn-sm btn-danger"
                                        onclick="return confirm('¿Desactivar este grado?')">
                                    Desactivar
                                </button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>
